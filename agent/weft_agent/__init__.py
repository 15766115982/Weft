"""Weft agent service (ADR-0012): LLM tasks + graph-constrained governance agent.

Successors the retired Node `llm/` service behind the identical CLI contract:
  python -m weft_agent <task> --kb <path> [--input-file f] [--output-file f]
"""
