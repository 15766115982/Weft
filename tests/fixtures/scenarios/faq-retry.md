# 常见问题:支付重试

## 重试失败了我的钱会扣两次吗?

不会。所有写接口都要求 Idempotency-Key,同一个 key 的重复请求直接返回第一次
的结果,不会重复扣款。

## 重试几次之后还不行怎么办?

最多重试三次。三次都失败后订单进入人工审核,状态为 PAYMENT_TIMEOUT_UNKNOWN,
不会自动继续扣款。

## 为什么会触发 RETRY_BUDGET_EXHAUSTED 告警?

5 分钟内超过 2% 的请求把重试预算打满就会告警,通常意味着下游银行通道抖动,
先看通道健康再决定是否切换备用通道。
