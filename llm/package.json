{
  "name": "kb-llm",
  "version": "0.1.0",
  "description": "LLM service: owns all Azure OpenAI calls behind a stable CLI contract (contract: ../../schema/contract.md)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/"
  }
}
